---
title: GPU的IO-aware特点
date: 2026-02-23 10:00:00 +0800
categories: [LLM, Gpu]
tags: [gpu]
pin: true
---


# Background
  FlashAttention 分块(tiling **减少 HBM 往返次数**)算 和 FlashInfer cache结构优化 都是在IO-aware层面优化。了解下Gpu结构下IO-aware一些特点。 


# Gpu结构

A100 为例
```
┌─────────────────────────────────────────────────────────────┐
│                        A100 GPU Die                         │
│                                                             │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐        │
│  │ SM0 │ │ SM1 │ │ SM2 │ │ SM3 │ │ SM4 │ │ SM5 │  ...    │
│  └─────┘ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘        │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐        │
│  │ SM6 │ │ SM7 │ │ SM8 │ │ SM9 │ │SM10 │ │SM11 │  ...    │
│  └─────┘ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘        │
│              ...  总共 108 个 SM  ...                       │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                    L2 Cache (40 MB)                    │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────┐┌──────┐┌──────┐┌──────┐┌──────┐  HBM2e 控制器   │
│  │ MC 0 ││ MC 1 ││ MC 2 ││ MC 3 ││ MC 4 │  连接显存       │
│  └──────┘└──────┘└──────┘└──────┘└──────┘                  │
│                                                             │
│  ┌─────────────────┐  ┌──────────────────┐                 │
│  │  GigaThread 引擎 │  │   PCIe / NVLink  │                │
│  │  (全局调度器)     │  │   (和CPU/其他GPU) │                │
│  └─────────────────┘  └──────────────────┘                 │
└─────────────────────────────────────────────────────────────┘
         ↕↕↕↕↕ 连接到外部显存芯片 ↕↕↕↕↕
┌─────────────────────────────────────────────────────────────┐
│              HBM2e 显存 (80 GB, 带宽 2 TB/s)                │
└─────────────────────────────────────────────────────────────┘
```
GigaThread 引擎 把 blocks 调度到 多 SM上。 

SM结构， SM多个 Sub-partitions 做warp (32 threads)调度。 
     data-parallel：`1 thread -> 1 element`，每个线程只做一个最小工作单元（1 个元素）。
    **gpu 本质是 海量线程 + 极高带宽 + 零开销切换，调度 实现高吞吐**。 硬件级零开销上下文切换，靠海量 warp 填满流水线空泡，核心是"多线程隐藏延迟"。

```
┌────────────────────────── SM (1个) ──────────────────────────┐
│                                                              │
│  ┌─ 子分区0 (Sub-partition 0) ─┐  ┌─ 子分区1 ─┐             │
│  │                              │  │            │             │
│  │  ★ Warp 调度器 0             │  │  ★ Warp 调度器 1         │
│  │    ↓ 每周期选 1 个 warp      │  │    ↓                     │
│  │  ★ 指令分发单元              │  │  ★ 指令分发单元          │
│  │    ↓ 把指令送到下面的管线     │  │    ↓                     │
│  │  ┌──────────────────────┐   │  │  ┌──────────────────┐   │
│  │  │ FP32 管线: 16 Core   │   │  │  │ FP32 管线: 16 Core│   │
│  │  │ INT32管线: 16 Core   │   │  │  │ INT32管线: 16 Core│   │
│  │  │ FP64 管线:  8 Core   │   │  │  │ FP64 管线:  8 Core│   │
│  │  │ Tensor Core: 1 个    │   │  │  │ Tensor Core: 1 个 │   │
│  │  │ LD/ST 管线: 8 单元   │   │  │  │ LD/ST 管线: 8 单元│   │
│  │  │ SFU 管线:   4 单元   │   │  │  │ SFU 管线:   4 单元│   │
│  │  └──────────────────────┘   │  │  └──────────────────┘   │
│  │  ┌──────────────────────┐   │  │  ┌──────────────────┐   │
│  │  │ 寄存器堆: 16384 个    │   │  │  │ 寄存器堆: 16384个 │   │
│  │  │ (32bit 每个)          │   │  │  │                   │   │
│  │  └──────────────────────┘   │  │  └──────────────────┘   │
│  └──────────────────────────────┘  └────────────────────────┘ │
│  ┌─ 子分区2 ─┐  ┌─ 子分区3 ─┐                               │
│  │  (同上)    │  │  (同上)    │                               │
│  └────────────┘  └────────────┘                               │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐    │
│  │          Shared Memory / L1  (四个子分区 共享)         │    │
│  └──────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────┘

关键点:
  • 4 个子分区 = 4 条独立管线 = 4 个 Warp 调度器
  • 每个调度器 每个时钟周期 独立选一个 warp 发射指令
  • 所以 SM 每周期最多同时执行 4 个 warp 的指令！
```

**Grid → Block → Thread** 的逻辑结构, Grid 的blocks 被调度到 多个SM. Block在SM内 用wrap 分批 来调度。 

```
代码（逻辑）                    GPU 硬件（物理）
═══════════════                      ═══════════════

  ┌──────────┐                       ┌──────────────────┐
  │  Kernel   │  ─── 1个kernel ───→  │  整个 GPU         │
  │  (函数)   │                       │  GigaThread 分发  │
  └──────────┘                       └──────────────────┘
       │
       ▼
  ┌──────────┐                       ┌──────────────────┐
  │   Grid   │  ─── 1个grid ────→   │  所有可用 SM       │
  │ (网格)    │                       │  (108个 SM, A100) │
  └──────────┘                       └──────────────────┘
       │
       ▼
  ┌──────────┐                       ┌──────────────────┐
  │  Block   │  ─── 1个block ───→   │  1个 SM            │
  │ (线程块)  │                       │  (block 不跨 SM)  │
  └──────────┘                       └──────────────────┘
       │
       ▼
  ┌──────────┐                       ┌──────────────────┐
  │  Warp    │  ─── 32个线程 ───→   │  子分区的执行管线   │
  │ (线程束)  │                       │  (真正的调度单位)  │
  └──────────┘                       └──────────────────┘
       │
       ▼
  ┌──────────┐                       ┌──────────────────┐
  │  Thread  │  ─── 1个线程 ────→   │  1个 CUDA Core     │
  │ (线程)    │                       │  (1拍执行1条指令)  │
  └──────────┘                       └──────────────────┘
```

# grid block warp 的调度



```
Block (256 threads)
  │
  │ 切成 8 个 Warp
  ▼
┌─────────────────────────────────────────────────────────────┐
│                          SM                                  │
│                                                             │
│  ┌─ 子分区0 ──────────────┐    ┌─ 子分区1 ──────────────┐  │
│  │ 调度器0                 │    │ 调度器1                 │  │
│  │ 管辖: W0, W4            │    │ 管辖: W1, W5            │  │
│  │                         │    │                         │  │
│  │ 每周期:                 │    │ 每周期:                 │  │
│  │ 1.从 W0/W4 中选就绪的   │    │ 1.从 W1/W5 中选就绪的   │  │
│  │ 2.读该 warp 的寄存器    │    │ 2.读该 warp 的寄存器    │  │
│  │ 3.送到对应管线执行      │    │ 3.送到对应管线执行      │  │
│  │ 4.结果写回该 warp 寄存器 │    │ 4.结果写回该 warp 寄存器│  │
│  │                         │    │                         │  │
│  │ FP32管线 ████████(16)   │    │ FP32管线 ████████(16)   │  │
│  │ INT32管线████████(16)   │    │ INT32管线████████(16)   │  │
│  │ LD/ST管线████████(8)    │    │ LD/ST管线████████(8)    │  │
│  │ Tensor  ██(1)           │    │ Tensor  ██(1)           │  │
│  │ 寄存器堆 ████(16K×32b)  │    │ 寄存器堆 ████(16K×32b)  │  │
│  └─────────────────────────┘    └─────────────────────────┘  │
│  ┌─ 子分区2 ──────────────┐    ┌─ 子分区3 ──────────────┐  │
│  │ 调度器2                 │    │ 调度器3                 │  │
│  │ 管辖: W2, W6            │    │ 管辖: W3, W7            │  │
│  │ (结构同上)              │    │ (结构同上)              │  │
│  └─────────────────────────┘    └─────────────────────────┘  │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐    │
│  │   Shared Memory (四个子分区共享，Block 内所有线程可见) │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```
配置 Shared Memory 的大小来控制数据驻留在片上的量。 


# 存储量级


```
                    延迟           带宽            大小
                    ════           ════            ════
  ┌──────────┐
  │ 寄存器    │     ~1 cycle      ~20 TB/s        每线程 255 个 (32bit)
  │ Register │     ← 最快
  └────┬─────┘
       │
  ┌────▼─────┐
  │Shared Mem│     ~20 cycles    ~19 TB/s         每 SM 最大 228 KB (H100)
  │ 共享内存  │     ← 同 Block 的线程共享
  └────┬─────┘
       │
  ┌────▼─────┐
  │ L1 Cache │     ~30 cycles    ~12 TB/s         每 SM 128~256 KB
  │          │     ← 和 Shared Mem 共用物理空间
  └────┬─────┘
       │
  ┌────▼─────┐
  │ L2 Cache │     ~200 cycles   ~6 TB/s          全局共享 40~50 MB
  │          │
  └────┬─────┘
       │
  ┌────▼─────┐
  │ 全局显存   │     ~400 cycles   ~2 TB/s (A100)   80 GB (HBM2e)
  │Global Mem│     ← 最慢但最大
  └──────────┘
```

`FLOPs/Bytes`，判算力墙还是带宽墙。 
访问模式是否 coalesced（warp 内地址是否连续）。
做 tiling，把热点数据搬到 shared memory 复用。 是否存在“可不落地的中间大张量”。


# Cpu 和 Gpu 交互

kernel 例子： 
```cuda
__global__ void vecAdd(float* A, float* B, float* C, int N) {
    int i = blockIdx.x * blockDim.x + threadIdx.x;
    if (i < N) {
        C[i] = A[i] + B[i];
    }
}
```
每个元素做 1 次加法，但要 2 次读 1 次写

`__global__`：CPU 调用，GPU 执行。
`__device__`：GPU 调用，GPU 执行。
`__host__`：CPU 调用，CPU 执行（普通 C++ 函数）。


```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
阶段 1：CPU → GPU 传输数据
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CPU 内存                          GPU 显存 (HBM)
┌──────────┐    cudaMemcpy()     ┌──────────┐
│ A[0..N-1]│ ═══ PCIe/NVLink ══→│ d_A[0..N]│
│ B[0..N-1]│ ═══════════════════→│ d_B[0..N]│
│ C[0..N-1]│                     │ d_C[0..N]│ (未初始化)
└──────────┘                     └──────────┘
     带宽: PCIe 4.0 ≈ 32 GB/s，NVLink ≈ 600 GB/s

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
阶段 2：启动 Kernel，GigaThread 分发 Block
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

GigaThread 引擎收到任务: 3907 个 Block，每个 256 线程

                    3907 个 Block 排队
                    ┌──┬──┬──┬──┬──┬──┬──┬──┬──┬───────┐
等待分配:           │B0│B1│B2│B3│B4│B5│B6│B7│B8│ ...   │
                    └──┴──┴──┴──┴──┴──┴──┴──┴──┴───────┘
                         │
                    GigaThread 看哪个 SM 有空
                         │
                         ▼
           ┌──────────────────────────────────────┐
           │  SM 0: B0, B1  (每 SM 可以放多个 Block) │
           │  SM 1: B2, B3                         │
           │  SM 2: B4, B5                         │
           │  SM 3: B6, B7                         │
           │  ...                                  │
           │  SM 107: B214, B215                   │
           └──────────────────────────────────────┘

A100 有 108 个 SM，每个 SM 假设放 2 个 Block
→ 一波能处理 216 个 Block
→ 3907 / 216 ≈ 19 波 才能全部处理完

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
阶段 3：SM 内部执行（以 SM0 的 Block0 为例）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Block0 有 256 个线程 → 8 个 Warp

SM0 的 Warp 调度器开始工作:

┌──────────────── SM0 执行时间线 ────────────────────────┐
│                                                        │
│  Cycle 1-2:   Warp0 发射指令: LOAD A[0..31]           │
│               ↓ 要等 ~400 cycles 才能拿到数据           │
│                                                        │
│  Cycle 3-4:   调度器切→ Warp1 发射: LOAD A[32..63]     │
│               ↓ Warp0 还在等                            │
│                                                        │
│  Cycle 5-6:   调度器切→ Warp2 发射: LOAD A[64..95]     │
│                                                        │
│  ... 继续切换 warp 执行 ...                              │
│                                                        │
│  Cycle ~400:  Warp0 的 A[0..31] 到了！                  │
│               Warp0 发射: LOAD B[0..31]                 │
│               ↓ 又要等 ~400 cycles                      │
│                                                        │
│  ... 调度器继续切到其他 warp 干活 ...                     │
│                                                        │
│  Cycle ~800:  Warp0 的 B[0..31] 也到了！                │
│               Warp0 发射: FADD（32个 FP32 Core 同时算）  │
│               C[0]  = A[0]  + B[0]   ← Core 0 算       │
│               C[1]  = A[1]  + B[1]   ← Core 1 算       │
│               C[2]  = A[2]  + B[2]   ← Core 2 算       │
│               ...                                       │
│               C[31] = A[31] + B[31]  ← Core 31 算      │
│               32个加法在 1 个时钟周期内全部完成！           │
│                                                        │
│  接着: Warp0 发射: STORE C[0..31] → 写回显存            │
│                                                        │
└────────────────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
阶段 4：所有 Block 执行完毕，结果写回 CPU
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

GPU 显存 (HBM)                    CPU 内存
┌──────────┐    cudaMemcpy()     ┌──────────┐
│ d_C[0..N]│ ═══ PCIe/NVLink ══→│ C[0..N-1]│  结果拷回
└──────────┘                     └──────────┘
```


